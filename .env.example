# Base URL of the deployed backend API (no trailing slash). Leave unset for local dev,
# where Vite's proxy in vite.config.js forwards /api to http://127.0.0.1:8000.
VITE_API_URL=https://your-backend.up.railway.app
